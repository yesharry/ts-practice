import React, { Dispatch, SetStateAction, useState } from 'react';

interface PieProps {
  getData: Array<object>;
}

const PiePage = ({ getData }: PieProps) => {
  return <div>PiePage</div>;
};

export default PiePage;
